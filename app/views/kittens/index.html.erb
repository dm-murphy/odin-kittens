<% @kittens.each do |kitten| %>
  <ul>
    <li><%= kitten.name %></li>
    <li><%= link_to "Delete Kitten", @kitten, method: "delete", { confirm: "Are you sure?", disable_with: "Processing..." } %>
  </ul>
<% end %>
